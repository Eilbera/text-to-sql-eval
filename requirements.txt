# Qwen3.6-27B requires a recent transformers (released Apr 2026).
# Pin to known-good minimums; bump if your environment has newer.
transformers>=4.57.0
trl>=0.16.0
peft>=0.13.0
accelerate>=1.2.0
datasets>=3.2.0
bitsandbytes>=0.45.0      # 4-bit (QLoRA) quantization; Linux + NVIDIA only
math-verify>=0.5.0        # robust answer parsing/equivalence for accuracy_reward
latex2sympy2-extended>=1.0.0
# Optional but strongly recommended for fast GRPO generation on a big model:
# vllm>=0.7.0
